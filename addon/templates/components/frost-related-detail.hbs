{{#if isSelected}}
  {{#if persistedRouteName}}
    {{#frost-link persistedRouteName}}
        <div class='circle'>
          {{frost-icon icon='frost/close'}}
        </div>
        {{relatedRoute.alias}}
    {{/frost-link}}
  {{else}}
    {{#frost-link defaultRoute}}
        <div class='circle'>
          {{frost-icon icon='frost/close'}}
        </div>
        {{relatedRoute.alias}}
    {{/frost-link}}
  {{/if}}
{{else}}
  {{#frost-link relatedRoute.route}}
      <div class='circle'>
        {{frost-icon icon=relatedRoute.svgPath}}
      </div>
      {{relatedRoute.alias}}
  {{/frost-link}}
{{/if}}
