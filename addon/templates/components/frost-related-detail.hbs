{{#if (eq myTargetRouteName route)}}
    <div class='circle'>
      {{#if icon}}
        {{frost-icon icon=icon}}
      {{/if}}
    </div>
    {{yield}}
{{else}}
    <div class='circle'>
      {{frost-icon icon='frost/close'}}
    </div>
    {{yield}}
{{/if}}


